<?php

// Canonical module version. The release workflow validates this against the
// tag, so it is the one place the number is written.
if (!defined('PAYPERCUT_PLUGIN_VERSION')) {
    define('PAYPERCUT_PLUGIN_VERSION', '1.0.6');
}
